/**
 * Stable participation status values — the single source of truth so
 * components compare against `PARTICIPATION_STATUS.X` instead of
 * hardcoded strings scattered across the feature.
 */
export const PARTICIPATION_STATUS = {
  CONFIRMED: 'confirmed',
  CANCELLED: 'cancelled'
}
